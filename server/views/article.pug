<!DOCTYPE html>
html(lang="en")
    head
        include head.pug
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title= data.name
    body
        a(href="/" class="text-white p-4") Home
        div(class="container text-center")
            div(class="row")
                div(class="col-md")
                    div(class="article-image-card mt-4")
                        img(class="img-fluid" src=data.image)
                    div(class="article-card")
                        h1= data.name
                        span(class="credits mt-3")= `${data.date} - `
                            a(href=data.source target="_blank")= data.source
                        p(class="mt-2 article-content")= data.content